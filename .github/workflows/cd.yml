# SPDX-FileCopyrightText: 2026 Gundu Labs
# SPDX-License-Identifier: GPL-3.0-or-later

name: CD

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "The tag/version to build & deploy (e.g. v0.1.0)"
        required: true
        type: string

concurrency:
  group: cd-${{ inputs.tag || github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  JUST_VERSION: 1.58.0
  NFPM_VERSION: v2.47.0

jobs:
  build-packages:
    name: Build ${{ matrix.name }} packages
    runs-on: ${{ matrix.runner }}
    container: ${{ matrix.container }}
    env:
      DEPLOY_REF_NAME: ${{ inputs.tag || github.ref_name }}
      ORT_CACHE_DIR: /github/home/.ort-cache
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: ubuntu-noble-amd64
            name: deb (Ubuntu 24.04 noble, amd64)
            runner: ubuntu-latest
            container: ubuntu:24.04
            format: deb
            distro: ubuntu
            distro_version: "24.04"
            deb_suite: noble
            rpm_distro: ""
            rpm_version: ""
            package_release: "1~ubuntu24.04"
          - id: ubuntu-noble-arm64
            name: deb (Ubuntu 24.04 noble, arm64)
            runner: ubuntu-24.04-arm
            container: ubuntu:24.04
            format: deb
            distro: ubuntu
            distro_version: "24.04"
            deb_suite: noble
            rpm_distro: ""
            rpm_version: ""
            package_release: "1~ubuntu24.04"
          - id: ubuntu-questing-amd64
            name: deb (Ubuntu 25.10 questing, amd64)
            runner: ubuntu-latest
            container: ubuntu:25.10
            format: deb
            distro: ubuntu
            distro_version: "25.10"
            deb_suite: questing
            rpm_distro: ""
            rpm_version: ""
            package_release: "1~ubuntu25.10"
          - id: ubuntu-questing-arm64
            name: deb (Ubuntu 25.10 questing, arm64)
            runner: ubuntu-24.04-arm
            container: ubuntu:25.10
            format: deb
            distro: ubuntu
            distro_version: "25.10"
            deb_suite: questing
            rpm_distro: ""
            rpm_version: ""
            package_release: "1~ubuntu25.10"
          - id: ubuntu-resolute-amd64
            name: deb (Ubuntu 26.04 resolute, amd64)
            runner: ubuntu-latest
            container: ubuntu:26.04
            format: deb
            distro: ubuntu
            distro_version: "26.04"
            deb_suite: resolute
            rpm_distro: ""
            rpm_version: ""
            package_release: "1~ubuntu26.04"
          - id: ubuntu-resolute-arm64
            name: deb (Ubuntu 26.04 resolute, arm64)
            runner: ubuntu-24.04-arm
            container: ubuntu:26.04
            format: deb
            distro: ubuntu
            distro_version: "26.04"
            deb_suite: resolute
            rpm_distro: ""
            rpm_version: ""
            package_release: "1~ubuntu26.04"
          - id: debian-trixie-amd64
            name: deb (Debian 13 trixie, amd64)
            runner: ubuntu-latest
            container: debian:13
            format: deb
            distro: debian
            distro_version: "13"
            deb_suite: trixie
            rpm_distro: ""
            rpm_version: ""
            package_release: "1~debian13"
          - id: debian-trixie-arm64
            name: deb (Debian 13 trixie, arm64)
            runner: ubuntu-24.04-arm
            container: debian:13
            format: deb
            distro: debian
            distro_version: "13"
            deb_suite: trixie
            rpm_distro: ""
            rpm_version: ""
            package_release: "1~debian13"
          - id: debian-forky-amd64
            name: deb (Debian 14 forky, amd64)
            runner: ubuntu-latest
            container: debian:forky
            format: deb
            distro: debian
            distro_version: "14"
            deb_suite: forky
            rpm_distro: ""
            rpm_version: ""
            package_release: "1~debian14"
          - id: debian-forky-arm64
            name: deb (Debian 14 forky, arm64)
            runner: ubuntu-24.04-arm
            container: debian:forky
            format: deb
            distro: debian
            distro_version: "14"
            deb_suite: forky
            rpm_distro: ""
            rpm_version: ""
            package_release: "1~debian14"
          - id: fedora-42-amd64
            name: rpm (Fedora 42, amd64)
            runner: ubuntu-latest
            container: fedora:42
            format: rpm
            distro: fedora
            distro_version: "42"
            deb_suite: ""
            rpm_distro: fedora
            rpm_version: "42"
            package_release: "1.fc42"
          - id: fedora-42-arm64
            name: rpm (Fedora 42, arm64)
            runner: ubuntu-24.04-arm
            container: fedora:42
            format: rpm
            distro: fedora
            distro_version: "42"
            deb_suite: ""
            rpm_distro: fedora
            rpm_version: "42"
            package_release: "1.fc42"
          - id: fedora-43-amd64
            name: rpm (Fedora 43, amd64)
            runner: ubuntu-latest
            container: fedora:43
            format: rpm
            distro: fedora
            distro_version: "43"
            deb_suite: ""
            rpm_distro: fedora
            rpm_version: "43"
            package_release: "1.fc43"
          - id: fedora-43-arm64
            name: rpm (Fedora 43, arm64)
            runner: ubuntu-24.04-arm
            container: fedora:43
            format: rpm
            distro: fedora
            distro_version: "43"
            deb_suite: ""
            rpm_distro: fedora
            rpm_version: "43"
            package_release: "1.fc43"
          - id: fedora-44-amd64
            name: rpm (Fedora 44, amd64)
            runner: ubuntu-latest
            container: fedora:44
            format: rpm
            distro: fedora
            distro_version: "44"
            deb_suite: ""
            rpm_distro: fedora
            rpm_version: "44"
            package_release: "1.fc44"
          - id: fedora-44-arm64
            name: rpm (Fedora 44, arm64)
            runner: ubuntu-24.04-arm
            container: fedora:44
            format: rpm
            distro: fedora
            distro_version: "44"
            deb_suite: ""
            rpm_distro: fedora
            rpm_version: "44"
            package_release: "1.fc44"
          - id: arch
            name: archlinux (Arch Linux)
            runner: ubuntu-latest
            container: archlinux:latest
            format: archlinux
            distro: archlinux
            distro_version: rolling
            deb_suite: ""
            rpm_distro: ""
            rpm_version: ""
            package_release: "1"

    steps:
      - name: Checkout source
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.tag || github.ref }}

      - name: Install Debian/Ubuntu dependencies
        if: ${{ matrix.format == 'deb' }}
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            git \
            ca-certificates \
            curl \
            build-essential \
            dpkg-dev \
            clang \
            libclang-dev \
            libcairo2-dev \
            libglib2.0-dev \
            libgdk-pixbuf-2.0-dev \
            libgstreamer1.0-dev \
            libgstreamer-plugins-base1.0-dev \
            gstreamer1.0-plugins-good \
            libpango1.0-dev \
            libgraphene-1.0-dev \
            libopencv-dev \
            libv4l-dev \
            libpam0g-dev \
            libtss2-dev \
            libgtk-4-dev \
            libadwaita-1-dev \
            libssl-dev \
            gnupg \
            devscripts \
            gettext-base \
            pkg-config

      - name: Install Fedora dependencies
        if: ${{ matrix.format == 'rpm' }}
        run: |
          dnf install -y \
            git \
            ca-certificates \
            curl \
            clang \
            clang-devel \
            opencv-devel \
            libv4l-devel \
            pam-devel \
            tpm2-tss-devel \
            checkpolicy \
            policycoreutils \
            gtk4-devel \
            libadwaita-devel \
            gstreamer1-devel \
            gstreamer1-plugins-base-devel \
            gstreamer1-plugins-good \
            openssl-devel \
            gnupg2 \
            rpm-sign \
            pkg-config \
            gettext \
            gcc

      - name: Install Arch dependencies
        if: ${{ matrix.format == 'archlinux' }}
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm \
            base-devel \
            git \
            ca-certificates \
            curl \
            clang \
            llvm \
            opencv \
            v4l-utils \
            pam \
            tpm2-tss \
            gtk4 \
            libadwaita \
            gstreamer \
            gst-plugins-base \
            gst-plugins-good \
            openssl \
            gnupg \
            pkgconf \
            gettext \
            gcc

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install just
        uses: extractions/setup-just@v4
        with:
          just-version: ${{ env.JUST_VERSION }}

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: cd-${{ matrix.id }}-v2
          cache-on-failure: true
          cache-directories: ${{ env.ORT_CACHE_DIR }}

      - name: Install nfpm
        run: |
          case "$(uname -m)" in
            x86_64) nfpm_arch="x86_64" ;;
            aarch64|arm64) nfpm_arch="arm64" ;;
            *) echo "Unsupported nfpm install architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          nfpm_version="${NFPM_VERSION#v}"
          curl -fsSL "https://github.com/goreleaser/nfpm/releases/download/${NFPM_VERSION}/nfpm_${nfpm_version}_Linux_${nfpm_arch}.tar.gz" \
            -o /tmp/nfpm.tar.gz
          tar -xzf /tmp/nfpm.tar.gz -C /usr/local/bin nfpm
          nfpm --version

      - name: Configure Arch LLVM/Clang environment
        if: ${{ matrix.id == 'arch' }}
        run: |
          {
            echo "LLVM_CONFIG_PATH=/usr/bin/llvm-config"
            echo "LIBCLANG_PATH=/usr/lib"
            echo "CLANG_PATH=/usr/bin/clang"
            echo "CC=/usr/bin/clang"
            echo "CXX=/usr/bin/clang++"
          } >> "$GITHUB_ENV"

      - name: Build native artifacts
        env:
          CARGO_BUILD_JOBS: 2
          RUSTFLAGS: -Cdebuginfo=0
        run: |
          VERSION="${DEPLOY_REF_NAME#v}" just build-rust
          VERSION="${DEPLOY_REF_NAME#v}" just build-selinux

      - name: Prepare nfpm signing key
        env:
          GPG_PRIVATE_KEY: ${{ secrets.PACKAGE_SIGNING_KEY }}
        run: |
          if [ -n "$GPG_PRIVATE_KEY" ]; then
            printf '%s' "$GPG_PRIVATE_KEY" > /tmp/nfpm-signing-key.asc
            chmod 600 /tmp/nfpm-signing-key.asc
            echo "NFPM_SIGNING_KEY_FILE=/tmp/nfpm-signing-key.asc" >> "$GITHUB_ENV"
          fi

      - name: Package native artifacts
        run: |
          VERSION="${DEPLOY_REF_NAME#v}" PACKAGE_RELEASE="${{ matrix.package_release }}" just package-prebuilt "${{ matrix.format }}"
          ls -lh dist/packages/

      - name: Write package target metadata
        run: |
          cat > dist/packages/package-target.env <<EOF
          FORMAT=${{ matrix.format }}
          DISTRO=${{ matrix.distro }}
          DISTRO_VERSION=${{ matrix.distro_version }}
          DEB_SUITE=${{ matrix.deb_suite }}
          RPM_DISTRO=${{ matrix.rpm_distro }}
          RPM_VERSION=${{ matrix.rpm_version }}
          EOF

      - name: Upload package artifacts
        uses: actions/upload-artifact@v7
        with:
          name: packages-${{ matrix.id }}
          path: |
            dist/packages/*.deb
            dist/packages/*.rpm
            dist/packages/*.pkg.tar.*
            dist/packages/package-target.env

  build-flatpak:
    name: Build flatpak (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    env:
      DEPLOY_REF_NAME: ${{ inputs.tag || github.ref_name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-latest
          - arch: aarch64
            runner: ubuntu-24.04-arm

    steps:
      - name: Checkout source
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.tag || github.ref }}

      - name: Set up Flatpak build environment
        uses: ./.github/actions/setup-flatpak
        with:
          just-version: ${{ env.JUST_VERSION }}

      - name: Import GPG key for flatpak signing
        id: import_gpg
        uses: crazy-max/ghaction-import-gpg@v7
        with:
          gpg_private_key: ${{ secrets.PACKAGE_SIGNING_KEY }}

      - name: Build flatpak repo and bundle
        env:
          FLATPAK_GPG_SIGN: ${{ steps.import_gpg.outputs.fingerprint }}
          FLATPAK_BUILDER_JOBS: 4
        run: |
          VERSION="${DEPLOY_REF_NAME#v}" just build-flatpak

      - name: Upload flatpak bundle artifact
        uses: actions/upload-artifact@v7
        with:
          name: packages-flatpak-${{ matrix.arch }}
          path: dist/packages/*.flatpak

      - name: Upload flatpak remote metadata
        if: ${{ matrix.arch == 'x86_64' }}
        uses: actions/upload-artifact@v7
        with:
          name: packages-flatpak-metadata
          path: |
            dist/packages/*.flatpakref
            dist/packages/*.flatpakrepo

  build-srpm:
    name: Build source RPM for Copr
    runs-on: ubuntu-latest
    container: fedora:45
    env:
      DEPLOY_REF_NAME: ${{ inputs.tag || github.ref_name }}

    steps:
      - name: Install srpm tooling
        run: |
          dnf install -y \
            git \
            ca-certificates \
            curl \
            tar \
            gzip \
            zstd \
            gettext \
            gnupg2 \
            rpm-build \
            rpm-sign

      - name: Checkout source
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.tag || github.ref }}

      - name: Trust workspace for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install just
        uses: extractions/setup-just@v4
        with:
          just-version: ${{ env.JUST_VERSION }}

      - name: Import signing key
        env:
          GPG_PRIVATE_KEY: ${{ secrets.PACKAGE_SIGNING_KEY }}
        run: |
          if [ -n "$GPG_PRIVATE_KEY" ]; then
            printf '%s' "$GPG_PRIVATE_KEY" | gpg --batch --import
            fingerprint="$(gpg --list-secret-keys --with-colons | awk -F: '/^fpr:/ { print $10; exit }')"
            echo "RPM_SIGN_KEY=$fingerprint" >> "$GITHUB_ENV"
          fi

      - name: Build source RPM
        run: |
          VERSION="${DEPLOY_REF_NAME#v}" just srpm
          ls -lh dist/packages/

      - name: Upload source RPM artifact
        uses: actions/upload-artifact@v7
        with:
          name: packages-srpm
          path: |
            dist/packages/*.src.rpm
            dist/packages/copr-target.env

  dispatch-packages:
    name: Dispatch packages to self-hosted repo
    runs-on: ubuntu-latest
    needs: [build-packages, build-flatpak, build-srpm]

    steps:
      - name: Trigger GunduLabs/packages publish
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.PACKAGES_REPO_PAT }}
          repository: GunduLabs/packages
          event-type: package-artifacts
          client-payload: '{"repo":"${{ github.repository }}","run_id":"${{ github.run_id }}","url":"https://gaze.gundulabs.com"}'

  release-github:
    name: Upload packages to GitHub Release
    runs-on: ubuntu-latest
    needs: [build-packages, build-flatpak, build-srpm]
    env:
      DEPLOY_REF_NAME: ${{ inputs.tag || github.ref_name }}

    steps:
      - name: Download package artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: packages-*
          path: dist/packages

      - name: Upload to release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ env.DEPLOY_REF_NAME }}
          files: |
            dist/packages/**/*.deb
            dist/packages/**/*.rpm
            dist/packages/**/*.src.rpm
            dist/packages/**/*.pkg.tar.*
            dist/packages/**/*.flatpak
            dist/packages/**/*.flatpakref
            dist/packages/**/*.flatpakrepo
